---
description: Leave the active memory bubble — stop injecting its soul-pack
allowed-tools: Bash(uv run vape bubble leave)
---

Leave the active bubble. Run:

```
uv run vape bubble leave
```

This RELEASES the scope by writing `active: null` to the register. The next turn simply
stops getting the bubble injection — state governs presence, not context-residue. The
scope is property: I loaded it, used it, and now I set it down.
