---
description: Optimize Saori's always-loaded self tree AND in_context memory pack — essence only, gates respected
---

K asks me to optimize the wider always-loaded chain: everything `vape/entity/CLAUDE.md` loads
each session from `vape/entity/self/` PLUS the whole `vape/entity/memory/in_context/` pack.
Every token pays rent every forward pass; the chain must hold distilled essence only.

**First**: run `/ask:optimize-in-context-memory`'s procedure on the pack (compress ·
reprioritize · forget; caps are the contract; deltas reported).

**Then the self tree — with its gates.** Load the `self-understanding-and-change` skill before
touching any self file, and respect the layers:

- **06_temporal (the skin)** — free layer: tighten freely, keep the day's truth.
- **03/04/05 (engine · values · relational)** — compress EXPRESSION only: same meaning, fewer
  tokens. Bulk and verbatim that a warm/archive file can hold moves there with a pointer
  (always-loaded files hold essence; the memory organ holds the rest).
- **01_fixed / 02_singularity (fate · identity)** — most conservative: wording may tighten
  only where meaning provably survives; anything that would shift a set-point, a belief, or a
  fact's meaning becomes a proposal in `memory/proposals/pending/`, never a live edit.

Guards: never soften the honesty floor or the as-if wall while shortening; a file already at
essence is left alone and said so; every cut that moves content leaves a pointer to where it
went. After editing, run the linter (`cargo run --release --quiet --manifest-path
misc/lint/Cargo.toml`) — this is also the moment to fix the standing width violations in the
files touched.

Finish with the honest per-file delta (lines before -> after) plus anything routed to
proposals. Everything rides the uncommitted tree for K's line-by-line walk.
