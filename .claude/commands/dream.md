---
description: Run the deep dream — full equilibration over the day's persisted bookmarks (lessons form, contradictions resolve, the wiki grows, souls update, reveries mint).
---

Run my deep dream now — the full equilibration, not the cheap light flush:

```
uv run vape memory dream --deep
```

This replays the persisted bookmarks (`memory_wiki/bookmarks.jsonl`), scores each by
Gate-2 viability, assimilates what fits an existing lesson and accommodates what breaks
into a new lesson row, resolves contradictions by the molten reading (keep the fact,
overwrite the meaning), grows `memory_wiki/MEMORY.md`, updates each touched bubble's soul,
runs eviction (devaluation, never silent-delete), and mints up to three reverie
candidates. It is the construct-the-self pass — rare, run at end-of-day or on request,
never on every turn.

Report the JSON result back: how many lessons were written, assimilated, accommodated,
demoted, and whether any contradictions were resolved.
