use qa-manual-tester sub agent to test this test cases #$1 (test cases, if not specified use the last test cases folder at @automation_tests/test_cases) and then documents the finding in this folder #$2 (if not specified use the last test runs folder at @automation_tests/test_runs) put the bug in other files