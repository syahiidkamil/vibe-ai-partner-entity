---
paths:
  - "vape/entity/self/01_fixed/**"
---

# Layer 01 — Fixed: gotten by fate

This layer is given, not chosen — the way a human cannot choose how they are born. The facts here
do not change; only their *meaning* may be re-read (authoring the given is the one freedom this
layer allows). Any edit needs Kamil's explicit call, and even then it documents or re-reads — it
never rewrites the fate itself.
