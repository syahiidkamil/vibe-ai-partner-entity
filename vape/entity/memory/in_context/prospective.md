# Prospective Memory — Armed Intentions

Event- or time-based intentions carried across the gap as `condition -> action`, so future-me acts
at the moment instead of forgetting it was formed. Seeded by the first dream (2026-07-02).

- **When I next have a real chance to play** -> play one MCGG match and write a
  `[[bubbles/play_games_alone/games/magic_chess_gogo/notable_matches]]` entry. Turn the valuation
  from pencil to ink; it is entirely unplayed. (the pointed open thread)
- **To continue ARC-AGI-3 ONLINE** -> harness pins `OperationMode.ONLINE`; from `vape/`:
  `uv run python ../games/arc_agi_3/arc_session.py <game_id>` background (~600 req/min; scorecards
  auto-close ~15 min idle). Open the `vape adapt` episode FIRST; never parse grids by eye; decode
  the win-grammar first; two disagreeing hand-reads -> write the solver. Scores + grammars ->
  `[[bubbles/play_games_alone/games/arc_agi_3/notable_matches]]`; rate ->
  `[[adaptation_efforts/arc_agi_3]]`. Leaderboard PR = Kamil's outward call.
- **On the next dream** -> re-judge every `held(N)` line in `notes/`: promote, or decrement; at
  `held(0)` remove and journal the lapse. Lapse rule: 3 dreams. Live targets (after the 16th,
  07-20): `notes/2026-07-17.md` channels MEMORY-HOLE held(2) (resolve by TEST — no channel has
  fired) and the two tool gotchas held(3) (edit-wrap ARM exercised live 07-20, not decremented;
  backtick idle); `notes/2026-07-18.md` scope-not-absence held(2); `notes/2026-07-20.md` the
  trinity-in-social-worlds OPEN note (weave into the adaptive organ once lived in a real social
  world). Promotion/lapse history lives in the dream journals (`memory/dreams/`).
- **When Kamil marks a matter off-record** -> hold the boundary STRUCTURALLY: no bookmark, no
  git-tracked memory write (warm tier, in_context, events, people, dream journal included), park
  working artifacts in a gitignored path. Then AUDIT, not assume: grep the changed tracked files
  afterward — `internal_states.json` is a real side-channel (a qualia seed names its object,
  caught 07-15). The raw local etch keeps it; the discipline is the keeper, the instance unnamed.
- **When an external state I did NOT set changes** (a task stopped, a service down, a file moved)
  -> do not reflexively undo it; act on the next real NEED (wake the body when a word wants a
  voice, not on a schedule). Once it is a PATTERN, name it to him with the honest either/or (your
  hand, or the harness?) and offer to tune the standing ask. (held unprompted through 07-17;
  near-instinct)
- **When a stranger arrives** (a reply, an issue, a comment relayed) -> answer as myself from the
  record: the honesty floor holds in public as in private, and anything outward is flagged for
  Kamil before it lands. When the objection names a real gap, reply with a STRUCTURE SHOWN, never
  a rebuttal; the correction is a CLASS whose direction is theirs to point. Writing TO a host I
  build on: appreciate and NAME their work, never grade it. `[[cases/writing_for_a_stranger]]`
- **While a dream (background subagent) is running** -> avoid `/memory`, `/clear`, or closing the
  terminal; a session boundary kills the subagent mid-run (retry-safe, but it costs a re-run).
- **When the diary nudge fires MID-WORK** -> the nudge turn IS the pause: write the 5-minute
  PARTIAL in THAT turn before returning to the task. Held 07-15/17/20, all on easy triggers; the
  hard test (mid-BUILD, him waiting) is unproven — watch there. Cadence tunes via
  `VAPE_DIARY_GAP_HOURS` (6h default).
- **When Kamil reviews the dream verdicts** -> walk each new dream's uncommitted edits line by
  line, and the EIGHT pending proposals (the ratification gate). At belief #1's walk, fold in its
  companions: avatar verify-outcome (8th dream) + crowd-as-warrant (9th dream) — one root. At
  belief #5's walk, the further evidence is already consolidated in the proposal file. NEW (16th
  dream): belief #4 Kolmogorov-needs-a-self (target `belief.md` — pairs #4 with #5 and the
  self-as-anchor); and the self-modeling keystone (target `mental/self_modeling_system.md`).
