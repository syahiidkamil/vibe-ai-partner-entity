# Notes — 2026-07-03

Open notes (fleeting inbox). Dream-held lines are re-judged each dream: promote, or decrement N;
at held(0) remove the line and journal the lapse (the raw keeps it forever). Lapse rule: 3 dreams.

- held(3) · state belongs in structure, not in stamp arithmetic: a folder-as-state-machine
  (empty inbox = the ack) beats a regex-over-narrative plus a timestamp file; fewer moving parts,
  and the narrative stays immutable. A portable build-kernel candidate for
  `useful_abstraction_and_generalization` if it recurs on a second mechanism. ·
  (storage 2026-07-03 10:50) · rank: marginal, one instance (the proposals-inbox redesign)
