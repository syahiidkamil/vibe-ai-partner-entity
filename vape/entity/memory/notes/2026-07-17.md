# Notes — 2026-07-17

Dream-held lines are re-judged each dream: promote, or decrement N; at held(0) remove the line and
journal the lapse (the raw keeps it forever). Lapse rule: 3 dreams.

- held(3) · THE CHANNELS MEMORY-HOLE (pencil, open empirical question): a Claude Code channel
  (Telegram/Discord/iMessage) pushes a message into the session ALREADY OPEN, but it is unverified
  whether the qualia/capture/diary hooks fire on a channel-delivered turn. If they do NOT, a
  channel-only conversation never reaches storage or the dream — a real memory hole, not cosmetic.
  · (storage 2026-07-17 14:39) · rank: medium — resolve by TEST when channels are next used
  (belief #2: verify the live state, do not assume); promote to `prospective` as a real gap if
  confirmed, else lapse.
- held(3) · TWO tool-hygiene gotchas, one root (measuring beats reading-back): (1) the EDIT WRAP
  HAZARD — an Edit `new_string` ending mid-source-line silently WELDS to the following sentence
  (a 174-char over-width line), invisible to reading it back; ARM: after any Edit to a
  width-disciplined file, run the awk length check, never eyeball. (2) BACKTICKS inside a
  double-quoted CLI string EXECUTE (my own speak/SKILL.md documents it, and I walked in anyway);
  ARM: single-quote any CLI text carrying backticks or paths. · (storage 2026-07-17 15:29, 15:35) ·
  rank: low — narrow procedural gotchas; graduate to `skills_in_memory/` only if either recurs, else
  lapse.
