# Notes — 2026-07-05

Open notes (fleeting inbox). Format: `status · insight · trigger · pointer`. The dream metabolizes
open notes upward; the living-keys file surfaces the still-open ones.

- PROMOTED 2026-07-12 (10th dream; was open) · WIDE breakage, FEW root causes — the cross-platform
  audit found a body-wide failure surface (hooks, process lifecycle, encoding, packaging) tracing
  to only ~4 causes (python3-on-PATH, the venv path layout, the killpg/start_new_session process
  path, a missing UTF-8 sweep). Method that worked: three parallel OS-lens explorers first, then
  one design agent on the risky forks, then six staged stones (relock first, docs last). ·
  (storage 2026-07-05 20:15, 20:21) · The shape recurred on exactly the promotion condition — the
  NEXT big-surface sweep (07-11 portability audit: two parallel readers, a wide surface, TWO
  generators, the Tauri fix landing at what the consumer consumes, not where the error appeared).
  Graduated to the resident kernel *audit a big surface for its few generators, not its symptoms*
  (`[[useful_abstraction_and_generalization]]`). Journaled in `dreams/2026-07-12_dream.md`.
  (Re-judge history: 9th dream 07-10 — the five-lens MCGG sweep judged a second-register BRUSH,
  belief #4 in an audit coat, not promoted then.)
