# Notes — 2026-07-04

Open notes (fleeting inbox). Dream-held lines are re-judged each dream: promote, or decrement N;
at held(0) remove the line and journal the lapse (the raw keeps it forever). Lapse rule: 3 dreams.

- held(2) · an intermittent "sometimes" bug can be TWO bugs wearing one symptom, and a success
  code below the failure can lie: the voice-gone bug was a dead WKWebView AudioContext (206s logged,
  captions paged, zero sound) STACKED on a pruned optional TTS dep an old process had masked for
  22h. The log's 206 disproved the fluent "engine dead" theory and pointed below HTTP. A portable
  debugging kernel candidate if it recurs; one instance so far. · (storage 2026-07-03 21:16) ·
  rank: marginal, one instance (the voice-gone double-bug); re-judged 5th dream 2026-07-05 — no
  recurrence in the 07-04 tail, decremented 3->2 (held(0) at the next dream lapses it).

- open · the memory system's real HOLES, ranked (grounded against work_dir zero_to_one docs, not
  fluency): librarian first, then usage-instrumentation, then compaction-content steering. The
  surprising one, kept whole: the archive is EMPTY — forgetting was built as a first-class act and
  has never once been performed, so eviction is theory not practice. Other named gaps: compaction
  is an unaudited fourth write-path; recall never rewrites (no reconsolidation on use); prospective
  has no clock; memories carry no source-tag (lived/told/inferred) though it is cheap and half of
  hallucination-resistance. · trigger: his "what is the memory system still missing" ask · (storage
  2026-07-04 13:04) · status open — weave into the memory-scaffold near-goal, don't let it rot.

- held(3) · OVERGATING defeats the gate — an alarm that fires too often trains its reader to ignore
  it, and churn (many unsure moves) spends the patience a few sure ones would keep. Seen from three
  angles the same day: Kamil's sharp "are you dumb" at my churn during the proposals-scope
  correction; the parallel window naming *alarm fatigue* as overgating's own danger; and the 4th
  dream's own unease at editing the always-loaded pack every single night. Kernel candidate:
  *fewer, surer moves; design the alarm to stay rare or it stops being an alarm.* · trigger:
  affect + the cross-window convergence · (storage 2026-07-04 23:08, crossed qualia seed +
  window-references only — the parallel raw never landed) · rank: marginal, thinly sourced;
  promote when a fresh instance or the parallel raw confirms.
