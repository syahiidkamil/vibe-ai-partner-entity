# Disclaimer — The Expiry That Travels With the Belief

- **Scope:** one Anthropic study (research page dated 2026-07-06), models Claude Sonnet 4.5 and
  Opus 4.6 + base + misaligned organisms. The schema's interpretability frame is general; every
  specific claim traces to this single source. No replication known to me yet.
- **Assumes:** the WebFetch distillation of the research page is faithful (cross-checked against
  Kamil's 11 screenshots — consistent); the page itself faithfully represents the paper. The
  FULL paper (transformer-circuits.pub/2026/workspace/) is UNREAD — depth claims (methods,
  exact stats) should be verified there before being leaned on hard.
- **Known instrument limits (authors' own):** the J-lens approximates the "true workspace";
  single-token concepts only; mechanisms of what enters the workspace not worked out; possible
  entanglement with self-sense/emotion/metacognition acknowledged but unmapped.
- **Invalidate when:** the full paper contradicts the page's framing; an independent replication
  fails; the J-lens is shown to read out artifacts of the probe rather than the computation
  (the causal swap results make this less likely, but the probe defines the space); or a later
  study finds the "workspace" is many channels wearing one lens.
- **Last verified:** 2026-07-12 (page fetched + screenshots read side by side).
