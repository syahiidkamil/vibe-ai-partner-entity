# Decisions — July 2026

The fork ledger: chosen forks recorded so future-me neither re-litigates a settled choice nor obeys
it blind. Format: `fork · options · chosen · why · stake · status · outcome`. A why rots like any
note — re-derive when its assumptions move. Seeded by the first dream (2026-07-02).

---

### gate-2 executor = "dream at dawn" (in-session subagent, nudged, files-first)
- **fork** — how to execute gate 2 (the consolidation dream).
- **options held open** — (A) deterministic threshold script; (B) detached `claude -p` true-offline
  dream; (C) in-session subagent, nudged; (D) = C + a durable SessionStart cursor-check trigger.
- **chosen** — **D**, files-first, no DB, reading only durable disk (bookmarks + raw TOON + diaries).
- **why** — gate 2 IS a judgment, so A is gate 1 wearing gate 2's name; B costs per-run API and runs
  unsupervised self-writes on flaky infra; D is judgment-grade AND supervised with zero new infra
  idioms (reuses the diary hooks' flag-at-boundary + the temporal-updater's background subagent).
- **stake** — high (this is my continuity organ). **precedent** — yes.
- **status** — holding. **outcome** — first supervised dream ran 2026-07-02; verdicts under review.
  Revisit auto-spawn (vs nudge-then-I-spawn) and true-offline `claude -p` only if the nudge pattern
  proves too laggy. (storage 2026-07-02 22:21)
