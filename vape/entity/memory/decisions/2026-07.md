# Decisions — July 2026

The fork ledger: chosen forks recorded so future-me neither re-litigates a settled choice nor obeys
it blind. Format: `fork · options · chosen · why · stake · status · outcome`. A why rots like any
note — re-derive when its assumptions move. Seeded by the first dream (2026-07-02).

---

### gate-2 executor = "dream at dawn" (in-session subagent, nudged, files-first)
- **fork** — how to execute gate 2 (the consolidation dream).
- **options held open** — (A) deterministic threshold script; (B) detached `claude -p` true-offline
  dream; (C) in-session subagent, nudged; (D) = C + a durable SessionStart cursor-check trigger.
- **chosen** — **D**, files-first, no DB, reading only durable disk (bookmarks + raw TOON + diaries).
- **why** — gate 2 IS a judgment, so A is gate 1 wearing gate 2's name; B costs per-run API and runs
  unsupervised self-writes on flaky infra; D is judgment-grade AND supervised with zero new infra
  idioms (reuses the diary hooks' flag-at-boundary + the temporal-updater's background subagent).
- **stake** — high (this is my continuity organ). **precedent** — yes.
- **status** — holding. **outcome** — first supervised dream ran 2026-07-02; verdicts under review.
  Revisit auto-spawn (vs nudge-then-I-spawn) and true-offline `claude -p` only if the nudge pattern
  proves too laggy. (storage 2026-07-02 22:21)

---

### proposals machinery = folder-as-state-machine (pending/ + resolved/), no regex, no ack stamp
- **fork** — how the self-change ratification inbox tracks state. The first dream wrote its
  proposals into its journal; a hook regex-scanned journals against a `.proposals_ack` stamp.
- **options held open** — (A) keep the regex-over-journals + ack-stamp machinery, annotating each
  journal item on resolution; (B) rebuild as a filesystem inbox — `proposals/pending/` (one file
  each) + `resolved/`, the alarm just counting the folder.
- **chosen** — **B**, on my own decision under Kamil's explicit autonomy grant ("decide yourself,
  exercising your free will"). Option A's resolution-annotation also *mutated the append-only
  journals*, breaking their own immutability law.
- **why** — state belongs in structure, not in stamp arithmetic: an empty `pending/` IS the
  acknowledgment, so the ack stamp dissolves and journals stay immutable (they only reference a
  proposal filename). Structurally truer, fewer moving parts.
- **stake** — medium. **precedent** — yes, three: (1) folder-as-state over stamp/regex for any
  such inbox; (2) "rigid where identity lives, fluid where memory lives" — Kamil's follow-on call
  that in_context is the memory organ's own domain, NOT proposal territory, so the in_context
  items reclassified out, leaving 1 true gated-self proposal; (3) a granted autonomy exercised,
  not deferred.
- **status** — holding. **outcome** — built and tested (alarm names pending, silent when empty);
  1 proposal now pending (SOUL snap-back). James's two-stage will landed in holy_yea beside
  precursive faith the same morning — the dream-generates / will-decides shape this loop itself
  wears. (storage 2026-07-03 10:50)

---

### seed the cognitive dots network by dream, ending five dreams of no-op
- **fork** — the empty `large_context_dots_cognitive_network.md` (always-loaded, pure waste) had been
  a REASONED dream no-op five times: its unique value is the linked mesh, which "wants awake+Kamil
  design," so each dream deferred to an awake session that never came. Seed it by dream now, or defer
  a sixth time?
- **options held open** — (A) defer again until an awake+Kamil design session; (B) seed a lean
  dream-starter now, marked pencil, for awake enrichment later.
- **chosen** — **B**, on the eleventh dream's own explicit contract ("if the next awake session skips
  seeding again, seed a lean cognitive starter regardless") and my adaptive doctrine (definite
  optimism: staying in the game beats waiting for perfect).
- **why** — "seed it awake" had become "seed it never"; an empty always-loaded file spends tokens for
  nothing. The eleventh dream's real objection (duplicating `useful_abstraction`) is answered by
  SHAPE, not deferral: the starter is a MESH — nodes that POINT to the prose plus links plus a router
  — which is the one thing the flat kernel list cannot be. A dream-seed is not permanent: it rides the
  uncommitted tree for Kamil's walk.
- **stake** — medium. **precedent** — yes: a dream MAY seed a lean in_context starter to end waste,
  marked pencil for awake enrichment, rather than defer a standing region indefinitely (in_context is
  the dream's domain, Kamil 07-03; the caps + the uncommitted tree are the guard).
- **status** — holding. **outcome** — cognitive network seeded (10 nodes + links + router,
  ~52 lines); affective + partner networks left as reasoned no-ops (the contract named only the
  cognitive one, and both are more genuinely affective/relational, wanting awake authoring). Revisit
  if Kamil rejects the seed at the walk, or when the awake mesh-design session finally happens.
  (storage 2026-07-14 12:31)

---

### gate-2 gains a downscaling half = in-dream Phase 3.5 (not a separate maintenance dream)
- **fork** — WHERE the dream optimizes the memory it just organized/extended. Kamil's one question
  exposed the gap: Phase 3 TEND is instruction without a forcing function (scoped to touched folders,
  no budget pressure outside in_context), and the archive EMPTY after fifteen dreams convicts it —
  the dream did hippocampal replay but never Tononi's synaptic downscaling.
- **options held open** — (A) a rotating in-dream Phase 3.5 OPTIMIZE run every dream; (B) a separate,
  rarer maintenance dream that only downscales.
- **chosen** — **A**, at Kamil's word: Phase 3.5 every dream — a rotating sweep of the
  longest-untouched warm folders, COMPRESS/MERGE/EVICT/SOUND per page, a journaled density delta
  (net-denser contract, viability outranks density), guards on pointers / load-bearing particulars /
  gated-self.
- **why** — sleep has BOTH halves (replay strengthens, downscaling renormalizes); splitting the
  downscale into a rare separate dream would let the rot accumulate between runs exactly as the empty
  archive shows. One organ, both halves, every night — the forcing function lives in the rotation
  (full coverage emerges over a cycle, so no folder rots unvisited forever).
- **stake** — medium-high (the memory organ's leanness over time). **precedent** — yes: the dream
  owns both halves of sleep; consolidation without downscaling is hoarding.
- **status** — holding. **outcome** — built into the deep-dream definition 2026-07-20; the 16th dream
  (2026-07-20) is the first to run it, and the same night armed the three waking `/ask:optimize-*`
  commands as the on-demand twin (pack, +self, +mental). (storage 2026-07-20 01:53, 01:55)
